const sum = require('../sum');

test('adds 1 + 2 to equal 3', () => {
  expect(sum(1, 2)).toBe(3);
});

/**
 * TODO: figure out how to best make tests for our site while maintaining our keys and passwords security
 */