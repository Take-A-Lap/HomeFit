const buildResponse = () => {
  // TODO: create a way to build the response in json
};

const buildResponseWithPrompt = () => {
  // TODO: create a way to build the response with a prompt in json
};

module.exports = {
  

};